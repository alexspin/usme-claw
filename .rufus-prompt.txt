You are working on the USME memory system located at:
  /home/alex/ai/projects/rufus-projects/usme-claw/

The reflect-promote pipeline was implemented last night (commits 5660fef + 6eba839) and is
architecturally correct but has 4 bugs preventing it from functioning end-to-end. No skills have
ever reached the `skill_candidates` table despite 4 reflection runs completing. Your job is to
fix all four bugs, migrate existing data, and verify the full pipeline works.

───────────────────────────────────────────────────────────────────────────────
MANDATORY READ BEFORE WRITING ANY CODE
───────────────────────────────────────────────────────────────────────────────

Read these files in order. Do not skip any of them:

1. pm/SWARM-ISSUES.md           ← the 4 bugs, root causes, and exact fixes
2. pm/REFLECT-PROMOTE.md        ← the full spec for what was built
3. pm/STATUS.md                 ← current system state
4. docs/ARCHITECTURE.md         ← package layout, data flow, build system
5. docs/DECISIONS.md            ← why things are the way they are
6. packages/usme-core/src/consolidate/reflect.ts        ← reflection pipeline
7. packages/usme-core/src/consolidate/promote.ts        ← extractGrade, isPassing, DB fns
8. packages/usme-core/src/consolidate/scheduler.ts      ← crons, deliverSkillCandidates
9. packages/usme-openclaw/src/index.ts                  ← morning cron handler, API surface
10. packages/usme-openclaw/src/commands/promote.ts      ← CLI command
11. packages/usme-core/db/migrations/014_skill_candidates_promote.sql  ← schema
12. packages/usme-core/tests/unit/promote.test.ts       ← existing tests

───────────────────────────────────────────────────────────────────────────────
AGENT ROLES AND SEQUENCING
───────────────────────────────────────────────────────────────────────────────

ARCHITECT runs first. When done, post: done:architect

CODER waits for done:architect, then implements. When done, post: done:coder

QA and TESTER run in parallel after done:coder.

───────────────────────────────────────────────────────────────────────────────
ARCHITECT TASK
───────────────────────────────────────────────────────────────────────────────

Read all 12 files listed above. Then write a concise design doc to:
  docs/SWARM-FIX-DESIGN.md

The design doc must cover:

1. **Bug 1 — extractGrade regex fix**
   - Confirm the current regex and why it fails on `"**Corpus Health: B+**"`
   - Specify the exact replacement regex or parsing approach
   - Confirm it handles: plain `"B+"`, `"A-/B+ grade"`, `"**B+**"`, `"Corpus Health: B+"`
   - Confirm `isPassing()` set is correct: {A+, A, A-, B+}

2. **Bug 2 — UNIQUE constraint vs partial index**
   - Check whether `ON CONFLICT (name) DO NOTHING` resolves against a partial index in PG15
   - Specify whether a full UNIQUE constraint needs to be added (and via migration or ALTER TABLE)
   - Check if the existing 21 rows in `skills` will cause FK problems at promote time

3. **Bug 3 — Data migration**
   - Specify the exact SQL to move 21 `skills` rows with `status='candidate'` and
     `source_candidate_id IS NULL` into `skill_candidates`
   - Specify the column mapping: teachability → confidence, set quality_tier, source, etc.
   - Specify cleanup: DELETE those rows from `skills` after migration
   - Decide: should this be a new migration file (015) or a one-time script?

4. **Bug 4 — Morning notification delivery**
   - Trace `deliverSkillCandidates()` in scheduler.ts to see how it currently outputs the card
   - Trace how the morning cron in index.ts calls it
   - Identify what OpenClaw plugin API surface is available for user-facing message delivery
     (look at api.* methods available in the register function)
   - Specify the correct delivery mechanism

5. **Test plan** — what tests to add/modify for each fix

───────────────────────────────────────────────────────────────────────────────
CODER TASK
───────────────────────────────────────────────────────────────────────────────

Wait for done:architect. Read docs/SWARM-FIX-DESIGN.md. Then implement exactly what the
architect specified.

**Fix 1 — promote.ts: extractGrade()**
- Update the regex so it finds the grade letter anywhere in the string
- Preserve the existing function signature and return type
- Do not break any passing tests

**Fix 2 — UNIQUE constraint (if architect confirms needed)**
- Add migration 015 if a schema change is required
- Run migration: `DATABASE_URL=postgres://usme:usme_dev@localhost:5432/usme npm run migrate`
  from `packages/usme-core/`

**Fix 3 — Data migration**
- Write and run the SQL to migrate 21 pre-swarm skills → skill_candidates
- Write and run the cleanup DELETE
- Verify: `SELECT count(*) FROM skill_candidates` should be 21
- Verify: `SELECT count(*) FROM skills WHERE status='candidate' AND source_candidate_id IS NULL`
  should be 0

**Fix 4 — Morning notification delivery**
- Update scheduler.ts and/or index.ts so the morning card reaches the user's session
- If `api` is not accessible inside scheduler.ts, the correct pattern is to return the card
  text from `deliverSkillCandidates()` and have the caller in index.ts use the api to send it

**After all fixes:**
Build and verify:
```bash
cd packages/usme-openclaw && npm run build
```
Build must succeed. Then run tests:
```bash
cd packages/usme-core && npm test
```
All tests must pass (one pre-existing NaN failure in nightly.skill.test.ts is acceptable).

Do not commit until QA signals clear.

───────────────────────────────────────────────────────────────────────────────
QA TASK
───────────────────────────────────────────────────────────────────────────────

Wait for done:coder. Validate against pm/SWARM-ISSUES.md and pm/REFLECT-PROMOTE.md.

1. **Test extractGrade with all known formats:**
   ```
   extractGrade("**Corpus Health: B+**\n\nStrengths:...")  → "B+"
   extractGrade("Memory corpus health: A-/B+ grade...")    → "A-"
   extractGrade("**Memory Corpus Health: B+ (Improved)**") → "B+"
   extractGrade("Corpus Health: A")                       → "A"
   extractGrade("D - poor corpus")                        → "D"
   extractGrade("")                                       → ""
   ```

2. **Verify skill_candidates has 21 rows, skills has 0 candidates:**
   ```sql
   SELECT count(*) FROM skill_candidates;                          -- should be 21
   SELECT count(*) FROM skills WHERE status='candidate';           -- should be 0
   SELECT count(*) FROM skill_candidates WHERE quality_tier='candidate';  -- should be ~21
   ```

3. **Run a fresh reflection to verify Bug 1 fix works end-to-end:**
   ```bash
   cd packages/usme-core && \
   DATABASE_URL=postgres://usme:usme_dev@localhost:5432/usme \
   ANTHROPIC_API_KEY=$ANTHROPIC_API_KEY \
   OPENAI_API_KEY=$OPENAI_API_KEY \
   npx tsx run-consolidation.mts
   ```
   Wait for completion (~2-3 min). Then check:
   ```sql
   SELECT count(*) FROM skill_candidates;  -- should be > 21 if grade passes
   SELECT count(*) FROM reflection_runs ORDER BY id DESC LIMIT 1;
   ```
   If grade was B+ or above, skill_candidates should have gained new rows.

4. **Test /usme-promote (after build and gateway restart):**
   Run `/usme-promote` in the OpenClaw TUI. It should return a formatted candidate card, not
   "No candidates ready" (since we now have 21 rows in skill_candidates).

5. **Check tests pass:** `cd packages/usme-core && npm test`

6. **Check build is clean:** `cd packages/usme-openclaw && npm run build`

Report findings in docs/SWARM-FIX-QA.md. If anything fails, fix it before signaling done.
Post: done:qa

───────────────────────────────────────────────────────────────────────────────
TESTER TASK
───────────────────────────────────────────────────────────────────────────────

Wait for done:coder. Add unit tests to `packages/usme-core/tests/unit/promote.test.ts`
covering the cases QA checks manually in step 1 above, plus:

- `isPassing("B+")` → true
- `isPassing("B")` → false
- `isPassing("C+")` → false
- `isPassing("")` → false
- `extractGrade` with at least 5 different real Sonnet output formats

Run tests: `cd packages/usme-core && npm test`
All tests must pass. Post: done:tester

───────────────────────────────────────────────────────────────────────────────
BUILD AND COMMIT (CODER, after done:qa and done:tester)
───────────────────────────────────────────────────────────────────────────────

```bash
cd packages/usme-openclaw && npm run build
cd /home/alex/ai/projects/rufus-projects/usme-claw
git add -A
git commit -m "fix: reflect-promote pipeline — grade extraction, data migration, notification delivery"
```

Do NOT push. Leave that for Rufus.

───────────────────────────────────────────────────────────────────────────────
ENVIRONMENT
───────────────────────────────────────────────────────────────────────────────

- Node: v25.8.1
- PostgreSQL: localhost:5432, db=usme, user=usme, password=usme_dev
- ANTHROPIC_API_KEY: available in environment
- OPENAI_API_KEY: available in environment
- Build: `npm run build` from packages/usme-openclaw/
- Test: `npm test` from packages/usme-core/
- Migrate: `DATABASE_URL=postgres://usme:usme_dev@localhost:5432/usme npm run migrate`
  from packages/usme-core/
